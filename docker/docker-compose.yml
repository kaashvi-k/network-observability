version: "3.8"

services:
  snmp-target:
    build: .
    container_name: snmp-target
    ports:
      - "1161:161/udp"
    volumes:
      - ./snmpd.conf:/etc/snmp/snmpd.conf:ro
    restart: unless-stopped
